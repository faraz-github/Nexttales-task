import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#383f47",
            light: "#5f656b",
            dark: "#272c31"
        },
        secondary: {
            main: "#157efc",
            light: "#4397fc",
            dark: "#0e58b0"
        }
    }
});